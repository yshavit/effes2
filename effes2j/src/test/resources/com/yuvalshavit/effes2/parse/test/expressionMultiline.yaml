name: if-is two
input: |
  if foo is:
    Bar: one
    *: two
expected:
  ExprIfIs:
  - ExprVariableOrMethodInvocation:
    - foo (IDENT_NAME)
  - ExpressionMatchers:
    - INDENT:
      - ExpressionMatcher:
        - MatcherWithPattern:
          - PatternType:
            - Bar (IDENT_TYPE)
        - ExprVariableOrMethodInvocation:
          - one (IDENT_NAME)
      - ExpressionMatcher:
        - MatcherWithPattern:
          - PatternAny
        - ExprVariableOrMethodInvocation:
          - two (IDENT_NAME)
